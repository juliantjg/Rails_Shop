<div class="container">
    <center> <h1> <%= @type %> </h1> </center>
    <br>
    <div class="row">
        <% @items.each do |item| %>
            <div class="col-md-3">
                <div style="height: 300px; background-color: silver;">
                    <h4> <%= item.name %>  </h4>
                </div>
            </div>
        <% end %>
    </div>
</div>